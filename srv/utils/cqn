function isProjected(columns, column) {
  if (!columns || columns[0] === "*") {
    return true;
  }
  return columns.some((c) => (c.ref && c.ref[0] === column) || c.as === column);
}

module.exports = { isProjected };
